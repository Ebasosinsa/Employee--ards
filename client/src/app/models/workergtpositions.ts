export interface workergtpositions {
  match(regex: RegExp): boolean;
  id_gt_worker_positions: number;
  name_gt_worker_positions: string;
  karta_gt_worker_positions: string;
  factors_gt_worker_positions: string;
}
